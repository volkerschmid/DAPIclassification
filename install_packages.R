setRepositories(ind=c(1,2))
install.packages("nucim")
# If you got an error message during installation of packages "tiff" and "fftw", you need to install some libraries.
# For example in Ubuntu or Debian, use sudo apt install libtiff5-dev libfftw3-dev
